# name: Deploy Staging

# on:
#   release:
#     types: [published]

# jobs:
#   deploy-staging:
#     name: Deploy to Staging
#     runs-on: ubuntu-latest
#     if: "!startsWith(github.event.release.tag_name, 'prod/')"
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "22"
#           cache: "pnpm"

#       - name: Setup pnpm
#         uses: pnpm/action-setup@v4
#         with:
#           version: 9

#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile

#       - name: Build
#         run: pnpm run build

#       # Add your staging deploy steps here (e.g. deploy to Vercel, AWS, etc.)
#       - name: Deploy to Staging
#         run: echo "Deploy to staging environment - add your deploy script here"
